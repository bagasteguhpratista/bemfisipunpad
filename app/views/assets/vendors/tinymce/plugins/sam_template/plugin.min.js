tinymce.PluginManager.add('sam_template', function(editor, url) {
    // Add a button that opens a window
    // editor.addButton('sam_template', {
    //   text: 'My button',
    //   icon: true,
    //   onclick: function() {
    //     // Open window
    //     editor.windowManager.open({
    //       title: 'Example plugin',
    //       body: [
    //         {type: 'textbox', name: 'title', label: 'Title'}
    //       ],
    //       onsubmit: function(e) {
    //         // Insert content when the window form is submitted
    //         editor.insertContent('Title: ' + e.data.title);
    //       }
    //     });
    //   }
    // });
  
    // Adds a menu item to the tools menu
    editor.addMenuItem('sam_template', {
      text: 'Sam Template',
      context: 'tools',
      icon: true,
      onclick: function() {
        // Open window with a specific url
        editor.windowManager.open({
          title: 'Sam Template',
          url: _www + '/src/assets/src/template/main.html',
          width: 800,
          height: 600,
          buttons: [{
              text: 'Close',
              onclick: 'close'
            }]
        });
      }
    });
  
    return {
      getMetadata: function () {
        return  {
          title: "Sam Template",
          url: "http://samdesign.com"
        };
      }
    };
  });
  